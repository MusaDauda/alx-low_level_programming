#include <stdio.h>

/**
 * main - this will test a function that prints if interger
 * is positive or negative
 * Return: 0 (Success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
