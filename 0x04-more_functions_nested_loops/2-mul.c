#include "main.h"
/**
 * mul - multipliers two integer
 * @a: One of interger to be multiplied
 * @b: Second integer
 * Return: The product of a and b
 */
int mul(int a, int b)
{
	int product;

	product = (a * b);
	return (product);
}
