#include "main.h"

/**
 * _abs - a function that computesthe absolute value of an integer
 * @n: n is any interger
 *
 * Return: interger
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
