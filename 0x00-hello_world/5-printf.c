#include <stdio.h>
/**
 * Program prints "with proper grammar, but the outcome is a piece of art,"
 * Return:0 (Success) when exited successfully
 */
int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
